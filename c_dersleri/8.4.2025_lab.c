#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
	//1.soru
	
	/*int *ptr,n,i;
	printf("eleman sayisini giriniz-->");
	scanf("%d",&n);
	ptr=(int*)malloc(n*sizeof(int));
	for(i=0;i<n;i++)
	{
		printf("%d.eleman-->",i+1);
		scanf("%d",*ptr);
	}
	free(ptr);*/
	
	//2. soru
	
/*	int *ptr,sayac=5,sayac1=0;
	while(sayac>0)
	{
		printf("not girmek istiyormusunuz->");
		scanf("%d",&sayac);
		
		if(sayac>0){
			sayac1++;
			ptr=(int*)calloc(sayac1,sizeof(int));
			scanf("%d",&*ptr);
		}
		else break;
		
	}*/
	

	return 0;
}